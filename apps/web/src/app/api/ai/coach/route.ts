// POST /api/ai/coach — send message to AI Coach, get response

export async function POST() {
  // TODO: ETAP 7 — build context from user data, call OpenAI, return insight
  return Response.json({ message: 'AI Coach — ETAP 7' }, { status: 501 })
}
